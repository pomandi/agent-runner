# Agent Runner - Environment Variables Example
#
# Copy this file to .env and fill in your values
#
# Usage:
#   cp .env.example .env
#   # Edit .env with your credentials
#   docker compose up -d

# =============================================================================
# REQUIRED - Claude Agent SDK
# =============================================================================
# Get token from: claude setup-token (valid for 1 year)
CLAUDE_CODE_OAUTH_TOKEN=your_oauth_token_here

# =============================================================================
# Temporal Configuration
# =============================================================================
TEMPORAL_HOST=temporal:7233
TEMPORAL_NAMESPACE=default
TEMPORAL_TASK_QUEUE=agent-tasks
TEMPORAL_DB_PASSWORD=temporal123

# =============================================================================
# Langfuse Monitoring (Optional but Recommended)
# =============================================================================
LANGFUSE_HOST=https://langfuse.pomandi.com
LANGFUSE_PUBLIC_KEY=pk-lf-xxx
LANGFUSE_SECRET_KEY=sk-lf-xxx

# =============================================================================
# AWS S3 - Product Images
# =============================================================================
AWS_ACCESS_KEY_ID=your_aws_access_key
AWS_SECRET_ACCESS_KEY=your_aws_secret_key
AWS_DEFAULT_REGION=eu-west-1
AWS_S3_BUCKET=saleorme

# =============================================================================
# PostgreSQL - Agent Outputs Database
# =============================================================================
DATABASE_URL=postgresql://user:password@host:5432/agent_outputs
POSTGRES_HOST=your_postgres_host
POSTGRES_PORT=5432
POSTGRES_DB=agent_outputs
POSTGRES_USER=your_user
POSTGRES_PASSWORD=your_password

# =============================================================================
# Meta (Facebook/Instagram) API - Pomandi Brand
# =============================================================================
META_ACCESS_TOKEN_POMANDI=your_long_lived_token
META_PAGE_ID_POMANDI=your_page_id
META_IG_ACCOUNT_ID_POMANDI=your_instagram_business_account_id

# =============================================================================
# Meta (Facebook/Instagram) API - Costume Brand
# =============================================================================
META_ACCESS_TOKEN_COSTUME=your_long_lived_token
META_PAGE_ID_COSTUME=your_page_id
META_IG_ACCOUNT_ID_COSTUME=your_instagram_business_account_id

# =============================================================================
# Application Settings
# =============================================================================
LOG_LEVEL=INFO
API_PORT=8080
RUN_MODE=worker
